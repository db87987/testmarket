	<div id="middle">
		<div class="middle-idx">
		
		<%= render :partial => 'spree/shared/taxonomies' %>
		
			<div class="right-idx">
				<ul class="bc">
					<%= breadcrumbs(@taxon) %> 
				</ul>
				
				<%
				  paginated_products = @searcher.retrieve_products if params.key?(:keywords)
				  paginated_products ||= products
				%>
				<% if products.empty? %>
				  <%= "Нет таких товаров - идите нахуй!" %>
				<% elsif params.key?(:keywords) %>
				  <h6 class="search-results-title"><%= t(:search_results, :keywords => h(params[:keywords])) %></h6>
				<% end %>

				<% if products.any? %>
				<ul class="catalogue-list cl-inside">
				<% products.each do |product| %>
				<% if Spree::Config[:show_zero_stock_products] || product.has_stock? %>
				
					<li class="<%= cycle("cl-item-first", "", "", "cl-item-four", :name => "classes") %>">
						<%= link_to small_image(product, :itemprop => "image"), product, :itemprop => 'url', :class => 'cl-img' %>
						<span class="cl-title"><%= link_to truncate(product.name, :length => 50), product, :itemprop => "name", :title => product.name %></span>
						<span class="cl-price"><%= number_to_currency product.price %></span>
						<a href="#" class="cl-buy">купить</a>
					</li>
					<% end %>
				<% end %>
				<% reset_cycle("classes") %>
				<% end %>
				</ul>
			</div>
		</div>
	</div>
	<div class="copyright">Allmusicmarket © 2012. Все права защищены, блять. Пошли все нахуй</div>
	<div class="footer-space"></div>
</div>