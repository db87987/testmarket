<% if menu_bar.present? %>
<ul class="l-menu">
<% menu_bar.menus.visible.each do |menu| %>
	<%= if menu.url == request.path
		content_tag(:li, content_tag(:span, menu.link_text))
		else
		content_tag(:li, link_to(menu.link_text, menu.url))
		end %>
  <% end %>
	
<% end %>